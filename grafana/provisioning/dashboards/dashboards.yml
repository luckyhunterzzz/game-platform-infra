apiVersion: 1

providers:
  - name: game-platform
    folder: Game Platform
    type: file
    disableDeletion: false
    updateIntervalSeconds: 30
    options:
      path: /etc/grafana/provisioning/dashboards/json
